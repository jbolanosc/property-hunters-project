import { ValidateSubmitDirective } from './validate-submit.directive';

describe('ValidateSubmitDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidateSubmitDirective();
    expect(directive).toBeTruthy();
  });
});
